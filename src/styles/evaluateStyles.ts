import { StyleSheet } from "react-native";

export const evaluateStyles = StyleSheet.create({
    inputArea : {
        marginBottom: 20,
    },
    input: {
        backgroundColor: "#fff",
        borderColor: "#666",
        borderWidth: 1,
        borderRadius: 8,
        padding: 10,
        fontSize: 16,
        color: "#666",
    },
});